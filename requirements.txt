# Core
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6
python-dotenv>=1.0

# Retrieval (default backend)
scikit-learn>=1.4
numpy>=1.26

# Agent graph
langgraph>=0.2
langchain-core>=0.2

# Ingestion
youtube-transcript-api>=0.6

# Speech (offline TTS, no API key needed)
pyttsx3>=2.90

# LLM providers (install what you use)
anthropic>=0.34
# openai>=1.30
# google-genai>=0.3

# Optional FAISS backend (RETRIEVER=faiss) — larger install, downloads a model
# sentence-transformers>=2.7
# faiss-cpu>=1.8
